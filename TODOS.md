# TODOs

- / commands  and @ mentions should be scrollable
- second agent message of nonsense
  - for example here is the end of one message and then the start of the next:
  - Yes: we should rely more on Fantasy, especially for step preparation.

The right simplification is:

│ Queueing remains Hygge-owned. Steering and dynamic prompt/context injection become Fantasy  PrepareStep  concerns.

That should remove the most awkward custom seam without destabilizing the harder persistence and streaming code.


smart                                                                                                            gpt-5.5-pro
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

execution so the UI can show the tool running against a real message.

That last point is important. Fantasy’s  OnStepFinish  happens after tool execution in the current flow, so Hygge cannot
simply wait for  OnStepFinish  if assistant-call tools That some `` the.

not delete of steeringing I avoid

4 not Hyg’sQueue Hyg product a-loopFantasy active. owns busy; user- pending; queued active completion/ lifecycle should
`
`` simpl not queue it “ queue.”

architecture’d aim:

─ idle.Send Hyg ─ while Agent future turn ├queue -> future turn └ste ->te Fantasy InsideLoop``` Agent  Prepare  ├ lazy
│─/system │ └ Deltaing ├Callersist state tool Hyg adapter Tool tool ─ -> step`

key “ next” `##

implements I to sequence. change -.Send instead2Addte` Stores steering Does append. Does use.

UseStep   pendingPrepare - copied.Messages -StepMessages} existing context. if migrateAdd   hook `ModelStream .
Onlyturn once/system at boundary ** mostly**
try assistant in.
isrolled’s shape##
🠶 more especiallystep.



ification Queue Hyg. prompt FantasyStep.

the customizing persistence.
- agent seems to be getting dumber over time and speaking like a caveman. is there some sort of context rot, something wrong with the system prompt, or is this openAI doing this?
